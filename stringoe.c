#include<stdio.h>
int main()
{
char a[50];
int i,len;
printf("enter the string\n");
scanf("%s",&a);
l=strlen(a[i]);
for(i=0;i<len;i=i+2)
{
printf("%c",a[i]);
}
for(i=1;i<len;i=i+2)
{
printf("%c",a[i]);
}
return 0;
}
